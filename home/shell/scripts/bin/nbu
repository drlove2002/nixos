#!/usr/bin/env bash
set -euo pipefail

if [[ "$(uname)" == "Darwin" ]]; then
  exec ~/.config/nixos/home/shell/scripts/bin/_nbu-darwin
else
  exec ~/.config/nixos/home/shell/scripts/bin/_nbu-nixos
fi
